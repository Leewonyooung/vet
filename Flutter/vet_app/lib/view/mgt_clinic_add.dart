import 'package:flutter/material.dart';

class MgtClinicAdd extends StatelessWidget {
  const MgtClinicAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        
      ),
    );
  }
}